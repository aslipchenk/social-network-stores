import Avatar from '@material-ui/core';

export const Home = () => {
  return (
    <div style={{ margin: '0 auto', marginTop: '50px', width: '980px' }}>
      Hello wrold
      <img src={''} alt={'hello wrold'} />
    </div>
  );
};

export default Home;
